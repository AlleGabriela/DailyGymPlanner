import 'package:flutter/material.dart';

// Color constants
const primaryColor = Color(0xff54026e);
const secondColor = Colors.yellow;
const buttonTextColor = Colors.black;
const questionTextColor = Colors.red;
const dropdownFieldColor = Color(0xffbcaaa4);
const accentColor = Colors.white;
const mealPagesColor = Colors.white;
const mealButtonColor = primaryColor;

const lightLila = Color(0xff9630B7);


// App sizing
const marginSize = 20.0;
const titleSize = 50.0;
const buttonHeight = 50.0;
const buttonWidth = 200.0;
const buttonText = 25.0;
const titleSizePhoto = 35.0;
const pageSizeName = 30.0;
const questionSize = 20.0;

const boxDataSize = 30.0; // boxes for sign in/log in pages


/* -- Strings -- */

// -- Welcome Screen Images (also used in Login & SignUp)
const String welcomeImage = "assets/images/welcome_image.png";
const String barbellImage = "assets/images/barbell.png";

// -- Fonts
const String font1 = "Righteous";
const String font2 = "Lobster";
